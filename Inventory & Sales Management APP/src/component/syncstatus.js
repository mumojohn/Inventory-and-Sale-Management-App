import React from 'react';

const SyncStatus = () => {
    return (
        <div>
            <h2>Sync Status</h2>
            <p>Last synced: Just now</p>
        </div>
    );
};

export default SyncStatus;